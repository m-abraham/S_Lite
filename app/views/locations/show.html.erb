<!-- Responsive panel example-->

<div class="row">
  <div class="col-sm-offset-1 col-sm-10 col-md-offset-2 col-md-8">
    <div class="panel panel-default">
      <div class="panel-heading">
        <h3 class="panel-title"><%= @location.name %></h3>
      </div>
      <div class="panel-body">
        <div class="row">
          <div class="col-md-12">
            <dl class="dl-horizontal">

            </dl>
            <div class="btn-group btn-group-justified">
              <a href="/locations" class="btn btn-primary">
                <i class="fa fa-chevron-left"></i>
              </a>

              <a href="/locations/<%= @location.id %>/edit" class="btn btn-warning">
                <i class="fa fa-edit"></i>
              </a>

              <a href="/delete_location/<%= @location.id %>" class="btn btn-danger" rel="nofollow">
                <i class="fa fa-trash-o"></i>
              </a>
            </div>
          </div>
        </div>
<!-- <ul>
  <% @location.users.each do |user| %>
    <li><%= user.email %></li>
    <li><img src= "<%= user.avatar %>" class="img-responsive>" height= "200"</li>
    <li><%= user.age %>
    <%= user.status %></li>
    <% end %>
    </ul> -->

    <div class="row isotope-container">
<% @location.users.each do |user| %>
  <div class="col-xs-12 col-sm-6 col-md-4 col-lg-3 isotope-item">
    <div class="panel panel-default">
      <div class="panel-heading">
        <h3 class="panel-title"><%= user.email %></h3>
      </div>
      <div class="panel-body">
      <img src= "<%= user.avatar %>" class="img-responsive>" height= "200">
        <dl>
        <dl>
          <dt>Age</dt>
          <dd><%= user.age %></dd>

          <dt>Status</dt>
          <dd><%= user.status %></dd>
        <% end %>
          <dl>
      </div>
    </div>
  </div>
</div>

<!-- End responsive panel example -->
